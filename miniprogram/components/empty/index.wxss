.empty { padding: 120rpx 40rpx; display: flex; flex-direction: column; align-items: center; color: var(--color-text-tertiary); }
.empty .icon { font-size: 96rpx; margin-bottom: 24rpx; opacity: 0.5; }
.empty .text { font-size: 26rpx; margin-bottom: 24rpx; }
